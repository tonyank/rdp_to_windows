name: G

on: [push]

jobs:
  build:
    runs-on: windows-2025
    timeout-minutes: 9999
    steps:

    - name: test
      shell: pwsh
      run: |
        echo "fuckdfhdfidhfdif" > 123.ISO
        mkdir temp
        move *.ISO temp/
        md5sum temp/*